https://codeforces.com/problemset/problem/50/A
solve1
#print(eval(input().replace(' ','*'))//2)
solve2
#while True:
    #size = input()
    #dimensions = size.split(" ")
    #try:
        #length = int(dimensions[0])
        #width = int(dimensions[1])
    #except ValueError:
       # pass
    #else:
        #break
 
#Find area
#area = length * width
#Find max dominoes
#dominoes = int(area/2 - (area%2)/2)
 
#print final answer
#print(dominoes)
solve3
#inp=[int(x) for x in input().split()]
#M=inp[0]
#N=inp[1]
#print((M*N)//2)